<p id="notice"><%= notice %></p>
<table class="table table-hover">
  <thead>
      <th align="center">
      <th>Name</th>
      <th>ID</th>
      <th>Date</th>
      <th></th>
      <th></th>
  </thead>

  <tbody>
    <% @dispatchers.each do |dispatcher| %>
      <tr>
        <td><%= dispatcher.name %></td>
        <td><%= dispatcher.id %></td>
        <td><%= dispatcher.created_at %></td>
        <td><%= link_to 'Show', dispatcher %></td>
        <td><%= link_to 'Edit', edit_dispatcher_path(dispatcher) %></td>
        <td><%= link_to 'Destroy', dispatcher, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Dispatcher', new_dispatcher_path %>

<table class="table table-hover">
<thead>
  <th> ID </th>
  <th> Van </th>
  <th>UO ID</th>
  <th> Rider Name </th>
  <th>Origin Address</th>
  <th>Destination Address</th>
  <th>Passengers</th>
  <th>Phone Number</th>
  <th></th>
  <th></th>
</thead>
<% @rides.each do |ride| %>
  <tr align="left">
    <td><%= link_to ride.id, ride_path(ride)%></td>
    <td align="left"><div class="btn-group">
      <button type="button" class="btn btn-default btn-xs dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
         <span class="caret"></span>
      </button>
      <ul class="dropdown-menu">
        <li><a href="#">1</a></li>
        <li><a href="#">2</a></li>
        <li><a href="#">3</a></li>
        <li><a href="#">4</a></li>
        <li><a href="#">5</a></li>
        <li><a href="#">6</a></li>
      </ul>
    </div> </td>
    <td><%= ride.sid%></td>
    <td><%= ride.name%></td>
    <td><%= ride.origin%></td>
    <td><%= ride.destination%></td>
    <td><%= ride.passengers%></td>
    <td><%= ride.phone%></td>
    <td align="right"><%= link_to "Edit", edit_ride_path(ride), class:"btn btn-default btn-xs" %></td>
    <td><%= link_to "Delete", ride_path(ride), class:"btn btn-danger btn-xs", method: :delete, data: { confirm: "Are you sure you want to delete this ride?" } %></td>

  </tr>
<% end %>
</table>
